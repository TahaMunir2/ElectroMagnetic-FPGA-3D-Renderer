# Source

Baseline source modules and reference code.

- `hdl/` - baseline SystemVerilog renderer/FDTD modules.
- `tb/` - module-level SystemVerilog testbenches, vector generators, and vector
  fixtures.
- `python/` - Python reference implementations.

Renderer design variants live in the top-level `design*/` folders and in
`D1S48/`.
